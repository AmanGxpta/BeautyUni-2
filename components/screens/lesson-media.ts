/**
 * Poster frame for the in-feed lesson clip — the still that sits behind the
 * phone mock in the hero and the showcase rail.
 *
 * Resized to 780px wide (2x the widest it is ever displayed) and saved as JPEG
 * rather than shipping the ~1MB source PNG for a background that is never seen
 * above 390 CSS pixels.
 */
export const LESSON_THUMB: string | undefined = "/lesson-thumb.jpg";
